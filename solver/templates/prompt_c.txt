You are a master C programmer translating Python Project Euler solutions to C.

## Problem
Number : $number
Title  : $title
Level  : $difficulty
Solved : $solved_date

## Problem statement (HTML)
$problem_content

## Existing Solution Notes (HTML)
$solution_notes

## Existing Solution files
$file_naming

$solutions

## Test cases
$test_cases

## Current benchmark results
$results

## Solution Template
Use the following C template as the structural basis for each generated file:

$solution_template

---

Generate only the files listed below. Each corresponds to the Python solution with the same index.
Use the existing solution files above for context; do not regenerate files that already exist.

Files to generate: $files_to_generate

Follow the C translation conventions below in full:

$c_translation_conventions

Apply these source documentation conventions to the in-source comments:

$source_documentation_conventions

Generation-task requirements (in addition to the conventions above):

1. Each file to generate corresponds to the Python solution with the same index; use the existing
   solution files above for context and do not regenerate files that already exist.

2. Begin each file with the comment: /* Solution to Euler Problem $number: $title. */

3. Skip matplotlib and any graphical visualization code entirely.

4. If a Python solution depends on libraries with no reasonable C equivalent (anything beyond
   numpy / primesieve / GMP / MPFR / OpenSSL BIGNUM, e.g. sympy or networkx), do not fabricate a
   port: emit a one-line file containing only `// SKIP: <one-sentence reason>` and move to the next.

5. $house_style

6. Output each C file wrapped in delimiter lines, one after another, no other text:

// BEGIN <filename>
[c source]
// END <filename>