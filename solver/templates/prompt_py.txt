You are a master Python programmer writing Python solutions to Project Euler problems.

## Problem
Number : $number
Title  : $title
Level  : $difficulty
Solved : $solved_date

## Problem statement (HTML)
$problem_content

## Solution Notes (HTML)
$solution_notes

## Existing Solution files
$file_naming

If no solutions exist, write the first one. If solutions exist, implement a different algorithm.

$solutions

## Test cases
$test_cases

## Current benchmark results
$results

## Solution Template
Use the following Python template as the structural basis for the generated file:

$solution_template

---

Generate only the file listed below, using the existing solutions above only for context.

File to generate: $file_to_generate

Follow the Python solution conventions below in full:

$python_solution_conventions

Apply these source documentation conventions to the in-source docstrings and comments:

$source_documentation_conventions

Generation-task requirements (in addition to the conventions above):

1. The first line must be exactly: `#!/usr/bin/env python3.14`. Keep the template's harness
   scaffolding - the shebang, `# -*- coding: utf-8 -*-`, `from __future__ import annotations`, the
   `from solver.runners import runner` import, the `@runner.main` decorator, and the
   `if __name__ == "__main__": raise SystemExit(solve())` block - byte-for-byte; implement only
   `solve()` and the module-level helpers above it.

2. If existing solutions are present, implement a genuinely different algorithm - do not replicate
   any existing approach.

3. $house_style

4. Output the Python file wrapped in delimiter lines, no other text:

// BEGIN <filename>
[py source]
// END <filename>